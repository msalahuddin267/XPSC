#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--)
    {
        int n, k;
        cin >> n >> k;

        int day[n], temp[n];

        for(int i = 0; i < n; i++)
            cin >> day[i];

        for(int i = 0; i < n; i++)
            cin >> temp[i];
    }
}
