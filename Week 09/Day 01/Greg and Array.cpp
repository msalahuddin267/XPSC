#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n, m, q;
    cin >> n >> m >> q;

    vector<ll> v(n);

    for(int i = 0; i < n; i++)
        cin >> v[i];


}
